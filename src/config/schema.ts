/**
 * Config schema — re-exports AltimeterConfigSchema with defaults.
 * Kept separate from types.ts for clean imports.
 */

export { AltimeterConfigSchema } from "../core/types.js";
export type { AltimeterConfig } from "../core/types.js";
