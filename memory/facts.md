# Persistent Facts

This file contains curated facts that persist across all sessions.

Add facts with: `altimeter memory add "your fact"`

## Project Facts

- Altimeter is a TypeScript AI agent orchestrator
- The agent loop is in src/core/agent-loop.ts
- Sessions are stored as JSONL files in ./sessions/
- Skills live in ./skills/<name>/SKILL.md
